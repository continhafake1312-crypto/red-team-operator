# F-037: Storefront slug enumeration + by-domain endpoint

**Alvo**: `api-beta.stormapplications.com`
**Data**: 2026-08-24T05:42:00Z
**Severidade**: 🔴 Alta (Info Disclosure via Swagger)

## Resultados

### Slug Enumeration

Slugs testados (14): admin, test, default, loja, storM, marketplace, store, shop, vendas, ticket, management, dashboard, painel, app

**Único slug confirmado**: `storm` (conhecido previamente)

Todos os demais slugs retornaram `UNKNOWN_STOREFRONT`.

### by-domain endpoint

| Método | Parâmetro | Resposta |
|--------|-----------|----------|
| GET | `?domain=stormapplications.com` | `{"code":"MISSING_HOST"}` |
| GET | `?domain=www.stormapplications.com` | `{"code":"MISSING_HOST"}` |
| POST | `{"domain":"stormapplications.com"}` | `{"code":"NOT_FOUND"}` |

O endpoint espera um formato de domínio diferente (possivelmente `domain=<slug>.<tld>` ou requer token de autenticação).

### [CRÍTICO] Wallet Swagger UI Descoberto

Em `wallet.stormapplications.com/api-docs`, um **Swagger UI completo** foi encontrado, expondo toda a documentação da API do StorM Wallet:

**Swagger JSON**: `wallet.stormapplications.com/api-docs/swagger.json` (31KB)
**OpenAPI Version**: 3.0.3
**API Title**: "StorM Wallet Gateway - API Completa"

**Endpoints documentados** (32 endpoints):
- Auth: `/auth/register`, `/auth/login`, `/auth/refresh`, `/auth/logout`, `/auth/me`
- Wallet: `/wallet`, `/wallet/balance`, `/wallet/pix-key`
- Transactions: `/transactions/create`, `/transactions`, `/transactions/recent`, `/transactions/{id}`, `/transactions/{id}/status`
- Withdrawals: `/withdrawals/create`, `/withdrawals`, `/withdrawals/{id}`
- KYC: `/kyc/submit`, `/kyc/status`
- Dashboard: `/dashboard/summary`, `/dashboard/sales`, `/dashboard/recent`
- Affiliate: `/affiliate/code`, `/affiliate/summary`, `/affiliate/referrals`, `/affiliate/commissions`
- API Keys: `/api-keys` (GET/POST), `/api-keys/{id}` (DELETE)
- Public API: `/api/v1/payments/create`, `/api/v1/account`, `/api/v1/payments`, `/api/v1/payments/{id}`, `/api/v1/withdrawals/create`, `/api/v1/withdrawals/{id}`
- Webhook: `/webhook/misticpay`

**Security Schemes**: BearerAuth (JWT) + ApiKeyAuth (X-API-Key header)
**Servers**: `http://localhost:3000` (dev), `https://api.stormwallet.com` (prod) — mas roda em `wallet.stormapplications.com`

## Impacto

A exposição do Swagger UI permite que qualquer atacante mapeie completamente a superfície de ataque da wallet API, incluindo endpoints que requerem KYC (documentos pessoais), pagamentos PIX, e gerenciamento de chaves de API.
