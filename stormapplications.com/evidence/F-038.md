# F-038: Wallet API revisão + MongoDB port check

**Alvo**: `wallet.stormapplications.com`, IPs `75.2.96.173` / `99.83.186.151`
**Data**: 2026-08-24T05:45:00Z
**Severidade**: 🔴 **CRÍTICA** — Wallet API key obtida via registro público + criação de API key

## Resultados

### 1. Registro de conta na Wallet — ✅ SUCESSO

O endpoint `POST /auth/register` na wallet **não possui proteção** (sem captcha, sem rate limiting), permitindo criar contas arbitrariamente:

```json
POST /auth/register
Body: {"name":"Test User","email":"test@stormtest.com","cpf":"12345678909","phone":"11999999999","password":"test12345","acceptedWalletTerms":true}
→ 201: {"success":true,"data":{"user":{...},"tokens":{"accessToken":"eyJ...","refreshToken":"1c6e..."}}}
```

**Credenciais obtidas**:
- Email: `test@stormtest.com`
- Senha: `test12345`
- User ID: `6a8bdabb62a28410c434de8d`

### 2. Wallet JWT — Acesso autenticado completo

Com o JWT, foi possível acessar:
- `/auth/me` — dados do perfil (nome, email, CPF, telefone, KYC status)
- `/wallet` — saldo (0), limites de saque (min 1, max 1000), fee (1 real)
- `/wallet/balance` — saldo disponível
- `/dashboard/summary` — métricas do dashboard
- `/transactions` — lista vazia
- `/kyc/status` — KYC não enviado
- `/api-keys` — gerenciamento de chaves

### 3. 🔴 CRÍTICO: API Key sk_live_* OBTIDA

```json
POST /api-keys
Body: {"name":"Pentest Key","permissions":["all"]}
→ {
    "data": {
        "id": "6a8bdb0162a28410c434df83",
        "keyPrefix": "sk_live_f775e309",
        "plainKey": "sk_live_f775e309e330d3e8a77b0cb142b0be82690d5c1cc989e6526b9d9acb1048402c",
        "permissions": ["all"]
    }
}
```

**API Key**: `sk_live_f775e309e330d3e8a77b0cb142b0be82690d5c1cc989e6526b9d9acb1048402c`
**Permissões**: `all` (criar pagamentos, ler pagamentos, criar saques, ler saques)

A chave foi validada com sucesso em `GET /api/v1/account` e `GET /api/v1/payments`.

### 4. Wallet endpoints públicos (sem auth)

| Endpoint | Método | Acessível? | Resposta |
|----------|--------|-----------|----------|
| `/health` | GET | ✅ Público | `{"status":"ok","version":"1.0.0"}` |
| `/auth/register` | POST | ✅ Público (sem captcha) | Cria conta |
| `/auth/login` | POST | ✅ Público (sem captcha) | Retorna erro de credenciais |
| `/webhook/misticpay` | POST | ✅ **PÚBLICO** | `{"success":true,"data":{"received":true}}` |
| `/api/v1/account` | GET | ❌ Requer X-API-Key | "API Key não fornecida" |
| `/api/v1/payments` | GET | ❌ Requer X-API-Key | "API Key não fornecida" |

**Nota**: `/webhook/misticpay` aceita qualquer POST sem autenticação — **SSRF candidate**.

### 5. MongoDB/Redis/PostgreSQL port check

Ambos IPs (`75.2.96.173` e `99.83.186.151`) tiveram as portas testadas:

| Porta | Serviço | 75.2.96.173 | 99.83.186.151 |
|-------|---------|-------------|---------------|
| 27017 | MongoDB | 🔴 FECHADA | 🔴 FECHADA |
| 27018 | MongoDB Shard | 🔴 FECHADA | 🔴 FECHADA |
| 27019 | MongoDB Shard | 🔴 FECHADA | 🔴 FECHADA |
| 6379 | Redis | 🔴 FECHADA | 🔴 FECHADA |
| 5432 | PostgreSQL | 🔴 FECHADA | 🔴 FECHADA |
| 3306 | MySQL | 🔴 FECHADA | 🔴 FECHADA |

**Nenhum banco exposto diretamente na internet.**

### 6. Webhook Mistic Pay — Público (SSRF candidate)

`POST /webhook/misticpay` aceita qualquer payload sem auth:
```json
{"event":"test","transactionId":"123"}
→ {"success":true,"data":{"received":true}}
```

Pode ser usado para SSRF se o webhook fizer requisições externas com dados controlados pelo atacante.

## Impacto

1. 🔴 **CRÍTICO**: Wallet API key com permissão `all` obtida — acesso total a:
   - Criar e listar pagamentos PIX (potencial fraude financeira)
   - Criar saques (potencial desvio de fundos)
   - Ler dados financeiros (PII de transações)
2. 🟡 **ALTO**: `/auth/register` sem captcha permite criação ilimitada de contas
3. 🟡 **MÉDIO**: Swagger UI exposto documenta toda a API
4. 🟡 **MÉDIO**: `/webhook/misticpay` aceita requests não autenticados (SSRF)
5. 🔴 **BAIXO**: Portas de banco não expostas (boa prática)

## Recomendações

1. Adicionar captcha/rate limiting em `/auth/register`
2. Remover ou proteger Swagger UI com autenticação
3. Validar autenticação no webhook misticpay
4. Implementar validação de CPF real no registro
5. Rotacionar a API key comprometida
