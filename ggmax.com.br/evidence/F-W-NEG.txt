# F-W-NEG — Vetores testados e descartados (negative results)

## Data: 2026-09-05T00:20Z (atualizado 00:40Z — Fase 6b)
## Host: api.keyz.gg + keyz.gg + ggmax.com.br

## Resumo de vetores testados que NÃO resultaram em vulnerabilidade confirmada.

### 1. JWT Secret Brute Force — DESCARTADO (secret forte)
- Ferramentas: hashcat -m 16500, Python hmac brute
- Wordlists testadas:
  - rockyou.txt (14.344.391 entradas) → ESGOTADO, sem sucesso
  - scraped-JWT-secrets.txt (103.979 entradas) → sem sucesso
  - custom keyz/ggmax wordlist (71 entradas) → sem sucesso
  - custom + best64 rules (5.467 combinações) → sem sucesso
- Conclusão: O secret JWT HS256 é forte/aleatório, NÃO é dicionário.
  Brute force não é viável sem GPU cluster.

### 2. Mass Assignment em POST /auth — DESCARTADO (DTO whitelist)
- POST /auth {email,password,role:"admin"} → 400 "property role should not exist"
- POST /auth {email,password,isAdmin:true,roleId:2} → 400 "property isAdmin/roleId should not exist"
- NestJS class-validator com whitelist:true + forbidNonWhitelisted:true.
- Campos privilegiados são rejeitados no DTO.

### 3. Mass Assignment em PATCH/PUT /me — DESCARTADO (rota não existe)
- PATCH /me → 404 "Cannot PATCH /me"
- PUT /me → 404 "Cannot PUT /me"
- Não há endpoint de update de perfil via /me.

### 4. /search Injection — DESCARTADO (parametrizado)
- q=' → retorna lista (LIKE wildcard quirk, não SQLi)
- q=' OR '1'='1 → vazio (parametrizado, sem SQLi)
- q=" OR "1"="1 → vazio (sem SQLi)
- q={{7*7}} → retorna produtos com "7" (substring match, NÃO SSTI)
- q={{7*'7'}} → retorna produtos com "7" (substring match, NÃO SSTI)
- q=${7*7}, q=#{7*7} → variável (substring match)
- q=<script>alert(1)</script> → vazio
- q={{constructor.constructor('return process')()}} → vazio (sem RCE via SSTI)
- Conclusão: /search usa query parametrizada + substring/LIKE match. Sem injeção.

### 5. OAuth Fake Tokens — DESCARTADO (valida com provider)
- POST /auth/google {token:"fake"} → 400 "Token not found" (valida com Google)
- POST /auth/discord {code:"fake"} → 400 "Invalid Discord code" (valida com Discord)
- POST /auth/twitch {code:"fake"} → 400 "Invalid Twitch token" (valida com Twitch)
- Não há bypass óbvio sem tokens OAuth válidos.

### 6. IDOR /tickets/{id} — DESCARTADO (owner-scoped)
- /tickets/1 a /tickets/60, /tickets/100 a /tickets/10000 → TODOS 404
- "Ticket not found" para qualquer ID (incluindo inexistentes e de outros)
- API escopa tickets ao usuário autenticado (bom protection).

### 7. IDOR /tickets/attachments/{id} — DESCARTADO (owner-scoped)
- /tickets/attachments/1 a /tickets/attachments/30 → TODOS 404
- Anexos escopados ao usuário.

### 8. IDOR /wishlist/{id} DELETE — DESCARTADO (owner-scoped)
- DELETE /wishlist/1, /wishlist/2, /wishlist/100 → TODOS 404
- "Wishlist item not found" (owner-scoped).

### 9. IDOR /orders/{id}/pay — INCONCLUSIVO (bug 500 mascara)
- POST /orders/{id}/pay com paymentMethod válido → 500 para TODOS os IDs
- O crash ocorre ANTES da verificação de ownership (ver F-W3).
- Não foi possível confirmar nem refutar IDOR.

### 10. None-alg JWT — DESCARTADO (já conhecido F-E5)
- JWT com alg:none → "jwt signature is required"

### 11. /reviews/{id} IDOR — DESCARTADO (sem rota individual)
- GET /reviews/{id} → 404 para todos (só /reviews collection existe)

### 12. /users/{id} — DESCARTADO (rota não existe)
- GET /users/1, /users/270, /users/me → "Cannot GET" (404, rota não exposta)

### 13. Password Reset — DESCARTADO (endpoint não exposto na API)
- /auth/forgot-password, /auth/reset-password, /auth/recover, /auth/forgot → TODOS 404
- Reset de senha não está na API NestJS (provável endpoint no app Nuxt front)

### 14. Coupons Brute — INCONCLUSIVO (nenhum encontrado)
- /coupons/validate?code={TEST10,PROMO10,DESCONTO,KEYZ,GGMAX,...} → TODOS 404
- Nenhum cupom válido encontrado na wordlist pequena (24 códigos).

---

## Fase 6b — Vetores adicionais testados (2026-09-05T00:40Z)

### 15. Nuxt server routes /api/accounts/search no keyz.gg — DESCARTADO (rota não existe)
- keyz.gg (Nuxt app) NÃO tem server routes /api/* (tudo delegado a api.keyz.gg/NestJS)
- /api/accounts/search → 404 "Page not found" (Nuxt)
- /api/users/v2/inspect/{user}/order-reviews → 404 (Nuxt)
- As rotas do wayback eram da API LEGADA do ggmax.com.br (PHP/Laravel), NÃO do Nuxt app
- CONFIRMADO: F-W7/F-W8/F-W9 encontrados na API legada (ggmax.com.br), não no keyz.gg

### 16. /conta/pedido/{id} (wayback, singular) no keyz.gg — DESCARTADO (rota removida)
- keyz.gg usa /conta/pedidos/{id} (PLURAL), não /conta/pedido/{id} (singular)
- Rota singular era da versão antiga do ggmax.com.br (removida na migração)
- Nova rota /conta/pedidos/{id} existe e chama GET /orders/{id} (API) — mas /orders retorna 500

### 17. /orders/{id} IDOR — INCONCLUSIVO (500 persistente mascara ownership)
- GET /orders/{id} retorna 500 para TODOS os IDs (wayback + numéricos) com auth
- GET /orders (lista) retorna 500 sem query params, mas 200 com ?offset=0&limit=10
  → {"orders":[],"total":0} (user 270 tem 0 orders)
- GET /orders/{id}?offset=0&limit=10 → ainda 500 (query params não ajudam)
- POST /orders/{id}/review → 500
- Tentativa de criar order real: TODOS os produtos "Out of stock" (staging sem inventário)
- Aceitei termos com CPF válido (60628869118) → 201 OK, mas criação falha (out of stock)
- IDOR não confirmado nem refutado — 500 bug ocorre ANTES do ownership check

### 18. /conta/pedido/{id} (wayback IDOR) no ggmax.com.br — UNTESTED (CF challenge)
- Páginas /conta/* no ggmax.com.br têm SEGUNDO CF challenge (mais estrito que /api/*)
- CF challenge "Verificando se você é humano" NÃO resolve via Playwright (page routes)
- cf_clearance do root bypass funciona para /api/* mas NÃO para /conta/* (segundo gate)
- IDOR via page route permanece não testado

### 19. Admin brute force (100k passwords) — EM ANDAMENTO (sem match após 2.4k)
- Wordlist: 100k-most-used-passwords-NCSC.txt (99.840 senhas)
- Target: POST /adm/auth {email:thyoity@gmail.com, password:FUZZ}
- Matcher: HTTP 200 + "validation" (senha correta retorna validation token)
- Rate: 5 threads, 0.2s delay, ~5 req/s via Tor
- Progresso: 2.404/99.840 (~2.4%) em 7 min — sem match
- ETA: ~4.6h para completar
- 70+ passwords direcionados do engagement testados (keyzgg@, variações, thyoity*, etc.)
  → TODOS retornaram 401 "Invalid password"
- Senha admin é FORTE (não é dicionário comum nem derivada do engagement)

### 20. /api/accounts/search SQLi — DESCARTADO (parametrizado)
- q=test' → "No account was found" (não vulnerável)
- q=test" OR "1"="1 → "No account was found" (parametrizado)
- q=test%27 → "No account was found" (URL-encoded, não bypassa)
- Endpoint usa query parametrizada (PDO/Laravel Eloquent)

### 21. /api/announcements — DESCARTADO (vazio)
- Retorna 200 com 0 bytes para todos os params testados (?page, ?limit, ?category_id, ?status)
- Endpoint existe mas não retorna dados (provável necessidade de auth ou params específicos)

### 22. API legada auth endpoints — DESCARTADO (não expostos)
- /api/auth/login, /api/login, /api/auth, /api/session, /api/auth/token → TODOS 404
- API legada não expõe endpoint de login (usa session cookies ou auth diferente)

### 23. /api/accounts/search partial/wildcard — DESCARTADO (exact match only)
- q=pat, q=kin, q=isra → "No account was found"
- Busca requer match EXATO do username (não suporta wildcards/partial)
